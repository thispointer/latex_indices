{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build LaTeX with Index",
            "type": "shell",
            "command": "./build_latex.sh",
            "args": [
                "${fileBasename}"
            ],
            "group": "build",
            "problemMatcher": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}